<% @categories.each do |c| %>
<section>
  <div class="<%= c.name %>">
    <h2>
      <%= c.name %>
    </h2>
    <ul>
  <% c.links.each do |l| %>
      <li>
        <%= link_to l.name, l.url %> 
      </li>
  <% end %>
    </ul>
  </div>
</section>
<% end %>
